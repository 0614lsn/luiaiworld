# 本轮结果：Edge 三站草稿交付按钮

- 计划：[实施计划](2026-09-12-unified-draft-delivery-plan.md)。
- 实现位置：main 的未提交工作树；本轮新增 `extensions/draft-delivery/`、本地 delivery/hub、公众号新增适配器和测试，更新使用文档；之前内容任务的未提交改动均保留。
- 机器验证：pass。44 项测试通过，三平台真实任务总状态 complete。
- 人类验收：pass。用户已安装扩展 0.1.3，并确认三站显示已核实，微信和小红书实际草稿箱均有内容。每期文章的公开发表审核仍由用户自行完成。

## 当前成果

统一入口为 Edge 扩展打开的交付页。服务监听 127.0.0.1:4389，持久保存三站状态；网站为本地稿，公众号用官方 API，小红书用指定 Edge profile 页面存稿。均不公开发表。首篇任务在 2026-09-12 21:08（本地时间）回写三项 draft_saved，目标绑定日常 Edge，8 张图片按顺序回读通过。

文档入口为 README、docs/content-workflow.md 与 content-distributor skill；平台实际回执保留在私人内容项目，连接码不进入公共文档或日志。

## 验证与返修

- 本地状态机测试覆盖双击复用、进程重启恢复、旧版本未知提交按 job ID 回读、图片顺序和正文核对、profile 不同、部分失败、人工稿保护、Host/Origin/token 及过大请求。
- 公众号模拟测试覆盖新增/已有人工稿保留、未知结果不重复新增、素材上传失败、回读截断与提示词缺失；生产环境重建草稿后正文 SHA-256 与旧定稿完全一致。
- 最终 `npm test`：44/44 通过，静态网站构建 5 页。覆盖页面重载后不得继续使用旧上下文、只点草稿按钮、无损重新编码和实际像素变化的区分，以及待核实任务防清理。随后补入微信 receipt 丢失时从操作记录恢复原 mediaId 的断言，受影响 6 项测试再次通过。
- `npm run check`：0 errors、0 warnings、1 hint。提示为 Chromium 当前仍用于编辑器兼容输入的 `document.execCommand('insertText')` 弃用标记；本轮已在 Edge 实际完成填写、保存与回读。
- 2026-09-12 首轮用户反馈：按钮没有完整成功。网站本地草稿成功；微信 `draft/get` 返回 40007，API 草稿列表为 0；用户确认原稿已发表，并明确允许以本地最后定稿重新建一篇验收草稿。已通过新增适配器建立一篇新草稿并回读，正文指纹保持一致，未公开发表。
- 小红书经历三项返修：保存/打开草稿会替换整页，需在新上下文回读；新页连续重载会中断只读查找，采用有界重连；平台重新编码 PNG 使原始字节不同，改为文件指纹相同或尺寸与全部 RGBA 像素相同的严格核验。最终 8 张均经像素核验通过，尺寸均为 1080×1440，未采用有损容差或模糊匹配。
- 真实最终证据：本地 `.content/delivery/jobs/<jobId>.json` 总状态 complete，具体任务 ID 见本期私人 project.json；三站均 draft_saved，微信 disposition 为 existing_preserved，正文指纹与最后人工定稿相同；小红书回执绑定 Edge 实例，标题、正文、8 图与顺序核验成功。回执详见私人项目 platforms 目录。
- 浏览器工具拒绝自动访问 `chrome-extension://` 扩展页，属于 URL 安全策略限制；由用户实际点击按钮，使用本地任务记录和允许访问的小红书页面核对，不使用替代通道绕过。

## 验收与交接

用户反馈：“现在，3个平台都显示已核实。我去小红书和微信公众号看了，也确实都有草稿了。”功能验收通过；本轮没有剩余阻断项。第三方页面结构、登录失效或平台有损转码仍可能使后续交付停在具体待处理状态，不会伪报成功。

README、长期工作流、项目 skill、旧计划/结果的后续入口及本期项目摘要已对齐。调试期关闭了明确属于本轮的内置浏览器检查页和一个早期 Edge 只读页，保留用户页面及当前草稿。扩展会复用自己的非编辑检查页；已打开供用户审核的编辑页不会强行关闭。

功能实施期间没有执行网站部署、公众号公开发表、小红书公开发布或删除原浏览器草稿。一次性微信排查脚本及“原草稿箱为空”的诊断结果移入 `.content/archive/unified-delivery-20260912/`；运行任务、连接码和实际回执保留。本地服务留运行，供用户后续使用；重开终端时运行 npm run content:hub。功能验收后，用户于 2026-09-12 明确授权将最新项目提交并推送到既有 GitHub 仓库；提交版本以 Git 历史为准，私人内容和运行数据仍不进入 Git。
