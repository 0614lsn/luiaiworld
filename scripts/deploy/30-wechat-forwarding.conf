# Allow the existing deployment user to reach only WeChat's official HTTPS API
# through local SSH forwarding. Global forwarding remains disabled.
Match User ecs-user
    AllowTcpForwarding local
    PermitOpen api.weixin.qq.com:443
Match all
